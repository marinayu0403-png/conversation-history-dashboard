# 📊 AI Avatar 對話分析儀表板 使用手冊

這份文件說明 `conversation_dashboard_v3.html` 網頁程式的作用以及如何操作使用。這是一個**完全在前端瀏覽器運行**的語意分析暨視覺化數據儀表板，專門用以分析聊天機器人（Agent）與用戶（User）之間的對話紀錄。

## 🎯 程式功能分析 (What it does)

本程式的主要功能如下：
1. **載入與解析 CSV**
   - 在本地解析包含對話紀錄的 CSV 檔，資料不需上傳雲端，直接在瀏覽器處理確保隱私。
   - 包含對欄位的自動偵測關聯（如 session_id、內容、說話者等），亦允許手動設定。
2. **數據總覽與視覺化 (Overview)**
   - 統計總 Session 數、訊息數、互動比例、對話輪數及說話者比例（USER vs AGENT）。
   - 透過 Chart.js 將對話呈現成圓餅圖與長條圖。
3. **語種分析 (Language Analysis)**
   - 利用內建的字元檢測規則（正規表達式）將用戶對話自動分類為日文、中文、英文/其他等語言。
   - 計算各語言的使用比例，以及 USER 與 AGENT 在各語言群體中的平均字數長度等統計差異。
4. **主題與關鍵字擷取 (Rule-based Topic Analysis)**
   - 透過內建「在地化」關鍵字分析引擎，透過預先設定的規則（例如住宿、溫泉、餐飲、交通等關鍵字）偵測每個對話的意圖與主題。
   - 統計頻出熱門關鍵詞。
5. **時間與用戶行為分析 (Time & Behavior Analysis) ✨新功能**
   - **時間分析**：洞察對話互動發生的高峰時段或日期走勢。
   - **用戶行為分析**：觀察特定用戶的話題延續性及異常活躍指標（Outliers）。
6. **自訂過濾器與排除關鍵詞 (Filter Keywords) ✨新功能**
   - 支援在載入資料時動態設定「排除關鍵詞」（如預設排除 `Athena` 等測試術語），過濾掉不需要分析的測試與噪音對話內容。
7. **報告建構與 AI 洞察匯出 (Report & AI Insights) ✨新功能**
   - 系統能自動提取關鍵數據，產生供您丟給大型語言模型（如 Claude 或 ChatGPT）的「提示詞 (Prompt)」。
   - 可將產出的文字結果記錄於「洞察區（Highlights / Warnings / Suggestions）」，並將圖表與文字打包**匯出一份高質感的 PDF 報告**。
8. **多國語言支援 (i18n)**
   - 內建在地化介面支援，可自由切換語言：繁體中文、英文、日文。

---

## 🚀 系統使用指南 (How to use)

### 步驟 1：開啟網頁
可以直接透過雙擊 `conversation_dashboard_v3.html` 檔案用任意現代瀏覽器（Chrome、Edge、Safari 等）開啟。不需要伺服器環境。

### 步驟 2：選擇介面語言與過濾條件
1. 開啟後，在左側邊欄上方可以選擇您屬意的介面語言（中文、EN、日本語）。
2. 在上傳區間，若資料中包含測試或不相干內部詞彙，可添加/維護**排除關鍵詞**（預設包含 `Athena`），藉此過濾特定對話。

### 步驟 3：準備與上傳資料
1. 點擊「**拖曳或點擊上傳 CSV**」區塊，或者左下角的「**上傳 CSV**」按鈕。
2. 準備您的 CSV 對話記錄。CSV 中最基本必須具備以下欄位：
   - **Session ID**：區分每次對話輪次的獨立識別碼。
   - **說話者 (Speaker)**：區分是客服還是用戶。例如欄位可填入 `USER` 或 `AGENT`。
   - **訊息內容 (Content)**：對話的具體文字內容。
   - *(可選)* **說話者 ID、訊息字數、建立時間等** 以獲得更有代表性的進階統計。

### 步驟 4：進行欄位配對
1. 上傳檔案後，系統會自動預測對應的欄位，請在畫面上出現的 **「確認欄位對應」** 區域核對。
   - AI 會自動分析辨識使用語言，您不需要自己產生語言欄位！
2. 如果欄位預設對應有誤，請手動下拉改變選項。
3. 確認無誤後，請點擊藍色按鈕 **「✓ 確認載入」**。

### 步驟 5：查看多維度分析報表
載入資料後，系統會在本地生成豐富的儀表板，您可以透過左側選單進入各區塊：

- **🏠 總覽 (Overview)**：查看所有 Session 與互動狀況的大盤數據及分佈圖表。
- **🌐 語言分析 (Language Analysis)**：查看用戶語系分佈，以及不同語言在訊息量長短的差異。
- **🤖 主題分析 (Topic Analysis)**：查看對話所提及的「常見服務主題」佔比（如設施、住宿、餐飲等）與熱門文字。
- **⏱ 時間分析 (Time Analysis)**：觀察互動發生的時間趨勢。
- **👤 用戶行為 (User Behavior)**：透過進階統計分析話題深度與找出異常行為。
- **💬 對話資料 (Sessions)**：能詳細篩選對話列表，點擊其中的紀錄可叫出「聊天回放對話框」完整還原情境。
- **📋 報告 (Report)**：在此取得數據總結所需的 AI 提示詞 (Prompt)，並將生成的洞察回填，最後一鍵下載成精美的 PDF 報告以利分享或歸檔。

### 📎 注意事項
- 因為運算皆在您的瀏覽器記憶體中執行，龐大的 CSV 檔案（超過萬筆以上訊息）在處理資料、或是 PDF 產生時，可能使畫面稍有停頓，此為正常現象，請耐心等待。
- 主題偵測是依賴腳本內建的正則邏輯詞庫 `TOPIC_RULES` 對應，若您需要自定義偵測主題，均可以直接修改該 `.html` 原碼文字檔。